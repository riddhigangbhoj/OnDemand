import { ServiceLanding } from './ServiceLanding';

/** Physical-training goal page — shared layout, training config. */
export function TrainingLanding() {
  return <ServiceLanding service="training" />;
}
